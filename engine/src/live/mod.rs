/// Compatibility namespace for the normalized live-event broadcast target.
/// No live runtime, IPC implementation, async runtime, or exchange dependency lives in `engine`.
pub mod ipc {
    pub const TO_ALL: u64 = 0;
}
